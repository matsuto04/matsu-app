class MessagesController < ApplicationController
  
  # def create
  #   message = Message.create(message_params)
  #   redirect_to tweet_path(message.tweet.id)
  # end

  # private
  # def message_params
  #   params.require(:message).permit(:tweet).merge(user_id: current_user.id, tweet_id: params[:tweet_id])
  # end
end
