.sideBar
  - current_user.groups.each do |group|
    .sideBar-list
      = link_to group_comments_path(group) do
        .sideBar-list__group   
          = group.name
        .sideBar-list__message
          = group.show_last_comment
  